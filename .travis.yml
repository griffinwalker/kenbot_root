dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.5"
  - "3.6" 
  - "3.7"
# command to install dependencies
install:
  - cd ..
  - git clone git@github.com:griffinwalker/kenbot_utils.git
  - cd reddit
  - pip install -r requirements.txt

script:
  - echo 'success'
